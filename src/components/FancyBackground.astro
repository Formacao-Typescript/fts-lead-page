<div class="background">
  <div class="background-images">
    <div class="img"><div class="img-overlay"></div></div>
    <div class="img"><div class="img-overlay"></div></div>
    <span class="overlay"></span>
    <!-- <img src="/assets/placeholder-bg-left-xl.png">
    <img src="/assets/placeholder-bg-right-xl.png"> -->

  </div>
  <div class="content-wrapper">
    <slot />
  </div>
</div>

<style lang="scss">

  .background {
    --bg-color-1: 4, 169, 204;
    --bg-color-2: 1, 100, 253;

    background: var(--bg-dark-purple);
    position: relative;
    overflow: hidden;
    z-index: 0;

    .content-wrapper {
      position: absolute;
      top: 0;
      left: 0;
      z-index: 5;
    }

    &-images {
      // width: 100%;
      // height: 100vh;
      // background :red;
      display: flex;
      justify-content: space-between;
      position: relative;

      .img {
        background-size: cover;
        &-overlay {
          width: 100%;
          height: 100%;
        }
      }
      > .img:nth-child(1) {
        width: 60%;
        height: 100svh;
        z-index: 2;
        background-image: linear-gradient(to bottom, rgba(var(--bg-color-1), 0.1), rgba(var(--bg-color-2), 0.3)),
    url('/assets/placeholder-bg-left-xl.png');
    // background-image: url('/assets/placeholder-bg-left-xl.png');

        .img-overlay {
          background: linear-gradient(to right, transparent 80%, black);
        }
      }
      > .img:nth-child(2) {
        width: 40%;
        height: 100svh;
        z-index: 1;
        // background: red;
        background-image: linear-gradient(to bottom, rgba(var(--bg-color-1), 0.2), rgba(var(--bg-color-2), 0.3)),
    url('/assets/placeholder-bg-right-xl.png');
    .img-overlay {
      background: linear-gradient(to left, transparent, black);
    }
      }

      .overlay {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: linear-gradient(to bottom, transparent, black);
        z-index: 4;
      }

    }
  }
</style>
