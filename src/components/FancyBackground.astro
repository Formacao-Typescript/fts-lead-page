<div class="background">
  <div class="background-images">
    <div class="img"><div class="img-overlay"></div></div>
    <div class="img"><div class="img-overlay"></div></div>
    <span class="overlay overlay-horizontal"></span>
    <span class="overlay overlay-vertical"></span>
  </div>
  <div class="content-wrapper">
    <slot />
  </div>
</div>

<style lang="scss">
  .background {
    --bg-color-1: 4, 169, 204; // #04a9cc
    --bg-color-2: 1, 100, 253; // #0164fd

    background: var(--bg-dark-purple);
    position: relative;
    z-index: 0;

    .content-wrapper {
      position: absolute;
      top: 0;
      left: 0;
      z-index: 5;
      width: 100%;
    }

    &-images {
      display: flex;
      justify-content: space-between;
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      z-index: -1;
      overflow: hidden; 

      .img {
        background-size: cover;
        height: calc(100svh - 50.5px);
        &-overlay {
          width: 100%;
          height: 100%;
        }

        @media (max-width: 768px) {
          height: calc(100svh);
        }
      }
      > .img:nth-child(1) {
        width: 60%;
        z-index: 2;
        background-image: linear-gradient(to bottom, rgba(var(--bg-color-1), 0.1), rgba(var(--bg-color-2), 0.3)),
          url('/assets/placeholder-bg-left-xl.png');

        .img-overlay {
          background: linear-gradient(to right, transparent 80%, black);
        }
      }
      > .img:nth-child(2) {
        width: 40%;
        z-index: 1;
        background-image: linear-gradient(to bottom, rgba(var(--bg-color-1), 0.2), rgba(var(--bg-color-2), 0.3)),
          url('/assets/placeholder-bg-right-xl.png');
        .img-overlay {
          background: linear-gradient(to left, transparent, black);
        }
      }

      .overlay {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: linear-gradient(to bottom, transparent, black);
        z-index: 4;

        &-horizontal {
          background: linear-gradient(to bottom, transparent, black);
        }
        &-vertical {
          background: linear-gradient(to right, transparent, black, transparent);
        }
      }
    }
  }
</style>
